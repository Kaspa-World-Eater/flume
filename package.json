{
  "name": "flume",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Pay-per-second streaming \u2014 audio and video metered by both sides and settled on Kaspa.",
  "bin": {
    "flume": "./bin/flume.ts"
  },
  "scripts": {
    "test": "node --import tsx --test \"src/**/*.test.ts\"",
    "serve": "tsx bin/flume.ts serve",
    "tune": "tsx bin/flume.ts tune"
  },
  "dependencies": {
    "metered-protocol": "^2.0.0",
    "@kaspa-x402/covenant": "0.1.0-alpha.10"
  },
  "devDependencies": {
    "@types/node": "^22.20.2",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
